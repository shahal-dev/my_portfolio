# Builds the Strapi CMS image in CI and pushes it to GHCR. The Oracle VM
# only has ~500MB RAM — nowhere near enough to build Strapi's admin panel
# bundle itself — so the VM only ever `docker compose pull`s this
# already-built image, never builds it locally. See docker-compose.yml and
# deploy.sh.

name: Build and push CMS image

on:
  push:
    branches: [main]
    paths:
      - 'cms/**'
      - '.github/workflows/build-cms.yml'
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: ./cms
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/my_portfolio-cms:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
